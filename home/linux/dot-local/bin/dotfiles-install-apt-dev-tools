#!/usr/bin/env bash
set -euo pipefail

packages=(
	clangd-22
	cmake
	g++-14
	g++-16
	gcc-14
	gcc-16
	postgresql-client
)

sudo apt-get install -y "${packages[@]}"

echo Ensured:
printf '\t%s\n' "${packages[@]}"
