#!/usr/bin/env bash
set -euo pipefail

dirs=(
	"$HOME/.config/git"
	"$HOME/.config/mpv"
	"$HOME/.local/bin"
	"$HOME/.source.d"
	"$HOME/.config/kitty"
	"$HOME/.ipython/profile_default"
	"$HOME/.vim/config"
)

install -d -m 700 "${dirs[@]}"
